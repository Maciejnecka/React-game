import React from 'react';
import { MapContainer } from './Map.styled';

const Map = () => {
  return (
    <MapContainer>
      {/* Tu będą umieszczane elementy gry jak Player, Monsters, Environment */}
    </MapContainer>
  );
};

export default Map;
